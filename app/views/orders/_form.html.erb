<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :order_price %>
  </div>

  <div class="field">
    <%= form.label :billingmethod %>
    <%= form.text_field :billingmethod, id: :order_billingmethod %>
  </div>

  <div class="field">
    <%= form.label :deliveryaddress %>
    <%= form.text_field :deliveryaddress, id: :order_deliveryaddress %>
  </div>

  <div class="field">
    <%= form.label :paid %>
    <%= form.check_box :paid, id: :order_paid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
