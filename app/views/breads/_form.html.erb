<%= form_with(model: bread, local: true, class: 'round-borders') do |form| %>
  <% if bread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bread.errors.count, "error") %> prohibited this bread from being saved:</h2>

      <ul>
      <% bread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
